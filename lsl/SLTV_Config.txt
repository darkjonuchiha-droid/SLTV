# SLTV configuration notecard. In-world, this notecard must be named: SLTV Config
# page_base = where the web assets live (GitHub Pages), no trailing slash
page_base=https://darkjonuchiha-droid.github.io/SLTV/web
# find yours with Edit -> Select Face on the screen side (Jon's 4x2.25x0.1 build: 2)
screen_face=2
# Camera Zoom aims automatically from screen_face on normal box prims.
# ONLY for mesh/odd builds where that misses: uncomment and set the direction
# the screen faces in the prim's local frame (+x/-x/+y/-y/+z/-z).
# screen_axis=+x
# channels: channel=Name|https://kosmi-room-url   (max 24; first 12 reachable via menu)
channel=Movies Night|https://app.kosmi.io/room/REPLACE-ME
channel=Music Hall|https://app.kosmi.io/room/REPLACE-ME
